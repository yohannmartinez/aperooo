import React from 'react';
import

const index = () => {
    return (
        <div>
            {
                games[randomNumber].game
            }
        </div>
    );
};

export default index;